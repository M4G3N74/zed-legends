// DJ speak modes
export const DJ_SPEAK_MODES = {
  ALWAYS: 'always',      // DJ can speak anytime
  BOOKENDS: 'bookends',  // DJ only speaks at beginning or end of tracks
  NEVER: 'never'         // DJ never speaks (text only)
};

// DJ feature flags
export const DJ_FEATURES = {
  AUTO_START: 'auto_start',
  SMART_RECOMMENDATIONS: 'smart_recommendations',
  VOICE_ENABLED: 'voice_enabled'
};